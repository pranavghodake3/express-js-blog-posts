extends master.pug

block pageLevelCss
    link(rel='stylesheet', href='/css/home.css')

block content
    each post in posts
        .row.home-posts
            .col-md-12.single-post
                h3.title #{post.title}
                div.description #{post.content}
                div.post-basic-info
                    div.at-time #{post.date} ago
                    div.post-by-user
                        strong #{post.user_id.first_name} #{post.user_id.last_name}
